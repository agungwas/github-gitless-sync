| Severity | Angle | File:line | Finding | Suggested fix |
|---|---|---|---|---|
| major | ux | src/settings/tab.ts:44-46 | Typing the first character into the trailing blank row flips it non-blank and immediately triggers `this.display()`, which does `containerEl.empty()` + full rebuild. The input's DOM node is destroyed, so focus is lost after exactly one keystroke — user must click back into the field to keep typing the rest of the pattern. | Preserve focus/caret across the redraw: after `this.display()`, re-focus the element at the same row index (or restore focus by element id/selector) and restore cursor position. |
| major | ux | src/settings/tab.ts:55-59 | Delete handler is `async` (`await onPatternsChanged()` does file/metadata I/O) before calling `this.display()`. If a user clicks a second delete button (different row) while the first click's await is still pending, that second closure's `index` was captured against the pre-splice array, so it splices the wrong entry once it runs — silently deletes an unintended pattern with no confirmation or undo. | Disable/ignore further row interactions (or re-render synchronously first, persist async after) while a delete is in flight; alternatively splice by pattern value/identity rather than closured numeric index. |
| major | ux | src/settings/tab.ts:37-48 | No visual feedback anywhere if a glob pattern is malformed/unparseable — per plan it's silently treated as non-matching, but the UI gives zero signal, so a user believes exclusion is active when it silently isn't. | Validate on `onChange`/blur and surface an inline error state (e.g. `Setting.setDesc()` with warning text or red input outline) when a pattern fails to parse. |
| major | ux | src/settings/tab.ts:245-261 | Only static copy explains "include always wins"; there's no live feedback when an include/exclude pair cancels out (e.g. a broad include already covers a new, more specific exclude). User adds an exclude pattern, sees no effect, and gets no clue why. | Show a live match indicator per row (e.g. "matches N tracked files" or "overridden by inclusion pattern X") so precedence conflicts are visible, not just documented in prose. |
| minor | ux | src/settings/tab.ts:45,58 | Every redraw triggered by finishing a pattern (line 45) or deleting one (line 58) calls `this.display()`, which rebuilds `containerEl` from scratch; this typically resets scroll position to the top of the settings tab. With a long exclude/include list this is disruptive — editing/deleting near the bottom bounces the view back up. | Capture `containerEl.scrollTop` (or the modal/tab scroll container's) before redraw and restore it after `this.display()` completes. |
| minor | ux | src/settings/tab.ts:52-54 | All trash buttons share the identical tooltip "Remove" with no per-row distinguishing text (e.g. the pattern value or row position). Uncertain whether Obsidian's `setTooltip()` maps to `aria-label` or just `title` — but even if it does set `aria-label`, screen reader users tabbing through several identical "Remove, button" controls can't tell which pattern each one deletes without cross-referencing the adjacent input separately. | Include the pattern value or row index in the tooltip/aria-label, e.g. `setTooltip(\`Remove "${pattern}"\`)`. |
| minor | ux | src/settings/tab.ts:37-40 | Each pattern row is built with only `addText(...)` — no `.setName()` is set on the `Setting`, so there is no persistent visible/accessible label for the row beyond the placeholder, which disappears from view once the field is filled and is not a robust accessible-name substitute across all browsers/AT. | Add a short `.setName()` (e.g. "Pattern") or an `aria-label` on the input itself so the row has a stable accessible name independent of placeholder/value state. |
| minor | ux | src/settings/tab.ts:37-61 | The trailing blank row has no delete button at all (rather than a disabled/reserved-space one), while every other row has one occupying the control area. This likely makes the last row's text input a different width than the rows above it, causing a visible layout shift each time a row transitions from "last" to "non-last". | Render a hidden/disabled trash button (same size, `display:none` visually or `setDisabled(true)`) on the trailing row instead of omitting it, to keep row widths consistent. |
| nit | ux | src/settings/tab.ts:26-28,42 | Clearing a non-last row's text to "" doesn't merge it with the true trailing blank row — it just becomes an extra blank row that happens to still have a delete button, indistinguishable at a glance from the true "add new pattern" row without one. | Consider visually marking the true trailing/add row differently (e.g. a subtle "+" affordance) so users can tell empty rows apart. |
| nit | ux | src/settings/tab.ts:259-261 | Inclusions placeholder example "e.g. gitless/**/main.js" assumes the reader understands why you'd re-include this specific plugin's own build output from within a folder literally named after the plugin — non-obvious rationale for a non-technical, note-taking audience. | Use a more generic, self-explanatory example, e.g. "e.g. important-notes/**" paired with the exclusions example. |
