| Severity | Angle | File:line | Finding | Suggested fix |
|---|---|---|---|---|
| blocker | security | src/sync-filters.ts:7-16, src/sync-filters.ts:23-27 | `globToRegExp` compiles user-controlled patterns into regexes with repeated `[^/]*`/`.*` segments (e.g. a pattern like `*a*a*a*a*a*a*a*a*a*a*a*ab`), a classic catastrophic-backtracking shape. `matchesAny`'s try/catch only guards `new RegExp()` compile errors (nearly unreachable since all metachars except `*`/`**` are pre-escaped) — it cannot interrupt a hanging/exponential `.test()` call, and this code runs synchronously on every file path at every sync tick, freezing the plugin/Obsidian main thread. | Cap pattern length/wildcard-segment count before compiling, and/or replace the backtracking engine with a linear-time glob matcher; add a matched-length or step budget so a pathological pattern degrades gracefully instead of hanging. |
| blocker | security | src/sync-manager.ts:1582-1591 | `removeExcludedFromMetadata()` (called on every keystroke via `tab.ts`'s `onPatternsChanged`) deletes any metadata entry whose path matches an exclude pattern, with no manifest exemption — unlike `shouldSkipFile`, `determineSyncActions`, and `isSyncable`, which all special-case `${vault.configDir}/${MANIFEST_FILE_NAME}`. A pattern that happens to match the manifest path (e.g. `.obsidian/**`) silently strips the manifest's metadata entry, breaking the plan's stated "manifest always synced" guarantee and risking metadata/sync-state corruption or data loss on the next sync. | Add the same manifest-path guard (`if (filePath === \`${vault.configDir}/${MANIFEST_FILE_NAME}\`) return;`) inside the `forEach` in `removeExcludedFromMetadata` before deleting. |
| major | security | src/settings/settings.ts:9-11 | `excludePatterns`/`includePatterns` are plain settings with no validation on load. When `syncConfigDir` is enabled, plugin settings live under `.obsidian` and can be synced through the shared repo, so any collaborator with repo write access can remotely plant a ReDoS payload or a manifest-matching exclude pattern into another user's client — the values are trusted as-is with no re-validation after being pulled from a remote/untrusted source. | Treat patterns loaded from synced settings as untrusted: re-apply length/complexity validation (see row 1) after every settings load, not just in the UI entry path. |
