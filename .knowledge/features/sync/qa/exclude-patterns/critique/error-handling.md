| Severity | Angle | File:line | Finding | Suggested fix |
|---|---|---|---|---|
| major | error-handling | src/sync-filters.ts:1 | `GLOB_SPECIAL_CHARS_REGEXP` omits `?`. A pattern where `?` ends up as the first char of a regex atom (e.g. exclude pattern `?`, or `**?foo`) makes `globToRegExp()` call `new RegExp()` on a string like `^?$`, which throws `SyntaxError: Invalid regular expression: Nothing to repeat` (verified with `node -e`). It's only non-fatal today because every current caller routes through `matchesAny`'s try/catch (sync-filters.ts:24-28) — `globToRegExp` itself is exported and offers no internal protection, so any future direct caller (e.g. a pattern-validation preview in the settings UI, or a test) will crash unhandled. | Add `?` to the escape char class, and/or wrap the `new RegExp(...)` calls inside `globToRegExp` itself in try/catch so "never throws" is a property of the function, not a convention callers must remember. |
| minor | error-handling | src/settings/tab.ts:35-37 | `onPatternsChanged` awaits `saveSettings()` and `removeExcludedFromMetadata()` with no try/catch; a rejection (e.g. disk write failure) becomes an unhandled promise rejection inside the `onChange`/`onClick` handlers with no user-visible feedback. This matches the existing "Sync configs" toggle handler (tab.ts:229-242), which has the same gap, so it's not a new pattern — but this new code fires on every keystroke instead of an infrequent toggle flip, multiplying how often the gap can be hit. | Wrap `onPatternsChanged`'s body in try/catch and surface failures via Obsidian `Notice`, at least for this new high-frequency codepath. |
| minor | error-handling | src/sync-manager.ts:1582-1598 | `removeExcludedFromMetadata()` deletes entries from `this.metadataStore.data.files` synchronously inside the `forEach`, then awaits `this.metadataStore.save()` once. If `save()` rejects (e.g. `adapter.write` fails, or the `writeQueue` chain in metadata-store.ts:75-81 is already poisoned by an earlier failed write), the in-memory deletions are never rolled back, so in-memory and on-disk metadata diverge, and the rejection propagates uncaught to the settings-tab caller (see row above). This mirrors the pre-existing `removeConfigDirFromMetadata` (sync-manager.ts:1551-1572), which has the same no-rollback shape (though it doesn't even await `save()`), so the failure mode itself isn't new — but it's now reachable on every pattern edit instead of only on an occasional config-dir toggle. | Catch `save()` failures and either revert the in-memory deletions or clearly log/report the divergence; consider debouncing pattern-driven metadata saves given the new per-keystroke call frequency. |
