| Severity | Angle | File:line | Finding | Suggested fix |
|---|---|---|---|---|
| major | perf | src/sync-filters.ts:20-25 | `matchesAny` calls `globToRegExp(trimmed)` fresh for every pattern on every call, with no memoization. `isExcludedPath` (built on `matchesAny`) is invoked per-file in `determineSyncActions` (src/sync-manager.ts:1180), per-event in `EventsListener.isSyncable` (src/events-listener.ts:192), per zip entry during extraction (src/sync-manager.ts:263), and inside every `shouldSkipFile` call site (src/sync-manager.ts:701,717,749,1477,1530). At vault scale (thousands of files) this means thousands of regex compilations per sync/scan pass, repeated per pattern, and re-triggered on every FS event during bulk operations. | Cache compiled patterns in a `Map<string, RegExp>` keyed by raw pattern string, built once per settings change (or lazily memoized and invalidated whenever `excludePatterns`/`includePatterns` are saved), so hot paths only do a map lookup instead of a fresh `new RegExp` construction. |
| major | perf | src/settings/tab.ts:46-52 (calls onPatternsChanged at line 37, which calls src/sync-manager.ts:1582 removeExcludedFromMetadata) | `onChange` fires `removeExcludedFromMetadata()` on every keystroke in any pattern row (not on blur/submit). That method iterates the entire `metadataStore.data.files` map and re-runs `isExcludedPath` (itself uncached, see above) against every tracked file. For a vault with thousands of tracked files, typing a single exclude pattern re-scans the whole metadata store per character, causing visible input jank and awaiting an async `metadataStore.save()` write per keystroke when a match changes. | Debounce the `onPatternsChanged` call (e.g. 300-500ms after last keystroke) or only trigger `removeExcludedFromMetadata()` on blur, not on every `onChange` firing. |
| major | perf | src/sync-manager.ts:1582-1596 | `removeExcludedFromMetadata()` is not guarded by `this.syncing` (used elsewhere at src/sync-manager.ts:95,441) and can be triggered from the settings tab (src/settings/tab.ts:37) while a `sync()` is mid-flight. It mutates `this.metadataStore.data.files` in place (`delete metadataStore.data.files[filePath]`) concurrently with `determineSyncActions`/`removeVolatileArtifactsFromLocalMetadata`/`filterRemoteMetadataFiles` (src/sync-manager.ts:701,717,749,1180,1477,1530) reading and writing the same object mid-sync, risking a torn snapshot (entries deleted after being read for diffing but before the sync action list is finalized, or a metadata save race overwriting concurrent in-progress changes). | Check `this.syncing` at the top of `removeExcludedFromMetadata()` and either skip/queue the cleanup until the in-flight sync finishes, or acquire the same mutex/flag guard used by `sync()` before mutating `metadataStore.data.files`. |
| nit | perf | src/sync-filters.ts:20-21 | `matchesAny` correctly short-circuits via `Array.prototype.some`, stopping at the first matching pattern — no change needed, noted only for completeness. | none |
